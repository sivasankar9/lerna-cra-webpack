import React, { useState } from "react";
import "./App.css";
import { Button, Text } from "@todcode/components";

function App() {

  console.log(Button);
  return (
    <div className="App">
      <Button onClick={()=>{}}  lable="Redirection"/>
      <Text lable="Redirection" />
    </div>
  );
}

export default App;
